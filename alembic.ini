# Use case: Configures versioned control-plane migrations.
# What it does: Locates migrations; the database URL comes from application settings.
[alembic]
script_location = migrations
prepend_sys_path = apps/api/src
path_separator = os
